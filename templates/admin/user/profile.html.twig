<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="{{ asset('assets/images/logo/logo-sm.png') }}" type="image/gif" sizes="16x16">
    <title>Mon Profil</title>
    <meta name="og:description" content="Profil utilisateur sur le tableau de bord Koteban.">
    <meta name="robots" content="index, follow">
    <meta name="og:title" property="og:title" content="Profil - Koteban">
    <meta property="og:image" content="{{ asset('assets/images/logo/koteban.PNG') }}">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="{{ asset('lib/bootstrap_5/bootstrap.min.css') }}">
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="{{ asset('lib/fontawesome/css/all.min.css') }}">
    <!-- main css -->
    <link rel="stylesheet" href="{{ asset('assets/css/global.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <!-- responsive css -->
    <link rel="stylesheet" href="{{ asset('assets/css/responsive.css') }}">
</head>

<body class="d2c_theme_light">

    <!-- Preloader Start -->
    <div class="preloader">
        <img src="{{ asset('assets/images/logo/logo.png') }}" alt="Koteban Logo">
    </div>
    <!-- Preloader End -->

    <!-- Profil Section -->
    <section class="d2c_login_system d2c_sign_up d-flex align-items-center">
        <div class="container">
            <div class="row">
                <div class="col-md-8 offset-md-2 col-lg-6 offset-lg-3 col-xxl-4 offset-xxl-4">
                    <div class="d2c_login_wrapper">
                        <div class="text-center mb-4">
                            <h4 class="text-capitalize">Mon profil</h4>
                            <p class="text-muted">Informations personnelles de votre compte</p>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Nom complet :</label>
                            <input type="text" class="form-control" value="{{ user.nom ~ ' ' ~ user.prenom }}" readonly>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Adresse email :</label>
                            <input type="email" class="form-control" value="{{ user.email }}" readonly>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Rôle :</label>
                            <input type="text" class="form-control" value="{{ user.roles[0]|replace({'ROLE_': ''})|capitalize }}" readonly>
                        </div>
                        <div class="mb-3">
                            <a href="{{ path('app_logout') }}" class="btn btn-danger w-100 text-capitalize">Déconnexion</a>
                        </div>
                        <div class="mb-3 text-center">
                            <a href="{{ path('admin_dashboard') }}" class="d2c_link text-decoration-none">← Retour au tableau de bord</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- JS -->
    <script src="{{ asset('lib/jQuery/jquery-3.5.1.min.js') }}"></script>
    <script src="{{ asset('lib/bootstrap_5/bootstrap.bundle.min.js') }}"></script>
    <script src="{{ asset('assets/js/main.js') }}"></script>
</body>
</html>
